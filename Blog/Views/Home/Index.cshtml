	@model	IList<Post>
<div>
    @if(Model.Count == 0)
                    {
                        @Html.Partial("_erro")
                    }
    else{
        @foreach	(var	p	in	Model)
	{
	    <article>
    	<h1>@p.Titulo</h1>
		<p>@p.Resumo</p>
		</article>
	}
    }
	
</div>